{
  "name": "multi-sport-ai-predictor",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "check": "node --check netlify/functions/predict.mjs && node --check netlify/functions/odds.mjs"
  }
}
